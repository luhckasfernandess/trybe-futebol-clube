const ANY_REQUIRED = 'All fields must be filled';
const STRING_EMAIL = 'Incorrect email or password';

const messageError = {
  anyRequired: ANY_REQUIRED,
  stringEmail: STRING_EMAIL,
};

export default messageError;
